version: "3.9"

services:
  brainzids:
    build: .
    container_name: brainzids_web
    ports:
      - "8081:8080"
    environment:
      - APP_NAME=${APP_NAME}
      - APP_CONTACT=${APP_CONTACT}
      - LIDARR_URL=${LIDARR_URL}
      - LIDARR_API_KEY=${LIDARR_API_KEY}
